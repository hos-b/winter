{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug",
            "type": "gdb",
            "request": "launch",
            "target": "./debug/gltest",
            "cwd": "${workspaceRoot}",
            "preLaunchTask": "cmake-debug"
        }
    ]
}